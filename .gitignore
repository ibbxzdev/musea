# dependencies
node_modules/
.pnpm-store/

# build output
.next/
out/
dist/
build/
.turbo/
*.tsbuildinfo

# rust / soroban
target/
**/*.rs.bk

# convex
.convex/

# env — NEVER commit. See .env.example for the shape.
.env
.env.*
!.env.example
!.env.local.example

# stellar testnet identities produced by scripts/ — these hold SECRET KEYS
.stellar/
*.stellar-keys.json
secrets/

# editors / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
._*

# logs
*.log
npm-debug.log*
pnpm-debug.log*

# test / coverage
coverage/
.vercel/

# Installed agent skills (third-party, large, each with its own license).
# skills-lock.json is committed and reproduces them: `npx skills add ...`
# Flip this if you'd rather vendor them for reproducibility.
.claude/skills/
.claude/stellar-data/
.agents/

# generated by Next on every build
apps/web/next-env.d.ts

# project docs — local only (SOW carries budget and contact details)
docs/
